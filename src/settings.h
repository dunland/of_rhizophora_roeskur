#define MIN_AREA_RELEASE 1 // decrease by this value each update
#define MAX_AREA_STEP 10   // decrease via trigger
#define MAX_AREA_RELEASE 1
#define THRESHOLD_STEP 40  // decrease threshold via trigger by this
#define THRESHOLD_MIN 50   // lower boundary of contour threshold
#define THRESHOLD_MAX 255
#define THRESHOLD_RELEASE 5 // increase threshold by this each update